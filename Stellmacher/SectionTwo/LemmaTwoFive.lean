module

public import Stellmacher.SectionTwo.LemmaTwoFour


open scoped BigOperators Pointwise

namespace Stellmacher.SectionTwo

universe u

/-! The in-Sylow representative of the normal 2-subgroup `V = ⟨Z^G⟩` and
its image under an automorphism of `S`, corresponding to the paper's
notation `V^τ`. -/
@[expose] public noncomputable def vSubgroupInSylow
    {G : Type u} [Group G] (S : Sylow 2 G) : Subgroup S :=
  (vSubgroup S).comap (S : Subgroup G).subtype

@[expose] public noncomputable def automorphismTranslateV
    {G : Type u} [Group G] (S : Sylow 2 G)
    (τ : MulAut S) : Subgroup G :=
  ((vSubgroupInSylow S).map (τ : S →* S)).map (S : Subgroup G).subtype

/-! **Stellmacher (2.5).**  The subgroup generated by all automorphism
translates of `V` is normal in `G` and lies in `O₂(G)`.  The quotient used in
the source is represented by a surjective homomorphism with the specified
centralizer kernel. -/
public theorem lemma_two_five
    {G : Type u} [Group G] [Finite G]
    (h : Hypotheses G) (S : Sylow 2 G)
    (hcharacteristic :
      ∀ K : Subgroup S, K.Characteristic → K ≠ ⊥ →
        ¬ (K.map (S : Subgroup G).subtype).Normal)
    (hunique :
      IsUniqueMaximalContaining (S : Subgroup G) (⊤ : Subgroup G))
    {barG : Type u} [Group barG] [Finite barG]
    (q : G →* barG) (hq : Function.Surjective q)
    (hker : q.ker = cSubgroup S)
    (hbar : Nonempty
      (barG ≃* Matrix.SpecialLinearGroup (Fin 2) (ZMod 2)))
    (T : Subgroup (MulAut S)) (hT : Odd (Nat.card T)) :
    ∃ K : Subgroup G,
      K = ⨆ τ : T, automorphismTranslateV S (τ : MulAut S) ∧
      K.Normal ∧ K ≤ pCore 2 G := by
  sorry

end Stellmacher.SectionTwo
