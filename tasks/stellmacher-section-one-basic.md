# stellmacher-section-one-basic — setup and Lemmas 1.1–1.3

Status: done
Target declarations: Section 1 action terminology, standing hypotheses/notation, and `Stellmacher.SectionOne.lemma_one_one` through `lemma_one_three`
Lean modules: `Stellmacher.SectionOne`
Sources: `refs/latex/stellmacher-n-group.tex`:L233-L312; `refs/files/stellmacher-n-group.pdf`: journal p. 14

## Task

Formalize the introductory action terminology, Section 1 standing hypotheses, corrected odd-core notation, and Lemmas 1.1–1.3; leave theorem proofs as `sorry`.

## Imports

- `Theory.GroupAction.Defs` / `Theory.GroupAction.Lemmas` — fixed-point and iterated action-commutator subgroups.
- `FeitThompson.PCore.PCore` — `O₂(G)`.
- `FeitThompson.PCore.Defs` — `O_{2'}(G)` as `pPrimeCore 2 G`.
- `Theory.ElementaryAbelian.Basic` — elementary abelian group predicates.
- `FeitThompson.Extraspecial` — the extraspecial alternative in Lemma 1.3(c).

## Bridges

- (none)

## Lessons

- (none)

## Resume (read this first — the recovery capsule)

- Current route: R3-lemma-one-three (done)
- Active route step: none; statement cluster is complete.
- Working node: none
- Current blocker: none; source finding `SF-stellmacher-1-odd-core` resolves the apparent `O₂` contradiction.
- Next action: Advance to the next Section 1 statement cluster when requested.
- Routes to avoid: using `pCore 2 G` for `W`; encoding `C_V(S₀)` as an ambient group centralizer rather than the fixed-point subgroup of the restricted action.
- Candidate next subnodes: none for this milestone.

## Progress

- observed: (2026-09-01T11:41:52Z) Scan audit confirms `C_G(V)=O₂(G)=1` but `W=O_{2'}(G)`; the TeX lost the prime only in the notation line.
- validated: (2026-09-01T11:45:24Z) Introductory action terminology, Section 1 hypotheses, corrected `oddCore`, and `LemmaOneOneConclusion` elaborate; `lake build Stellmacher.SectionOne` and the wrapper succeed with the expected statement-only warnings.
- validated: (2026-09-01T11:48:02Z) `actionClosure` and `lemma_one_two` elaborate and rebuild through the public wrapper.
- validated: (2026-09-01T11:51:14Z) `involutionCommutator`, `LemmaOneThreeConclusion`, and `lemma_one_three` elaborate; targeted owner and public-wrapper builds succeed with only the expected statement-only `sorry` warnings and the pre-existing `IsSolvable` deprecation warning.

## Proof Route

- Route id: R3-lemma-one-three
- Sketch: Define `F=[W,x]` as the ambient subgroup commutator of `oddCore G` with `⟨x⟩`. Quantify the prime `p` and its `IsPGroup p F` hypothesis explicitly. Encode `|V/C_V(x)|≤4` by the equivalent finite-cardinality inequality and the exact equality in alternatives (b),(c). Use an inductive conclusion for the three source alternatives, standard multiplicative cyclic models for `C₃`, `C₅`, and `C₃×C₃`, and `IsExtraspecial 3 F` plus order `3³` in (c).
- Main risk: central fixedness `[Z(F),x]=1` must map the center of the subtype `F` back into the ambient group before taking the subgroup commutator.

Steps:
- probe-statements: elaborate all terminology and four clauses in scratch.
  Direct consumer: `lemma_one_one`.
  Helpers: all planned subnodes.
- pin-module: create `Stellmacher/SectionOne.lean` and wire the wrapper.
  Direct consumer: `lemma_one_one`.
  Helpers: all planned subnodes.
- validate: build owner and wrapper; inspect expected `sorry` inventory.
  Direct consumer: `lemma_one_one`.
  Helpers: none.

## Subnodes

- `main` — done.
  Note: aggregate Lemma 1.1(a–d) without changing clause meaning.
  Dependencies: all helpers.
- `IsQuadraticAction` — done.
  Note: `[V,Y,Y]=1`.
  Dependencies: `commutatorAction₂`.
- `InducesTransvection` / `InducesTransvections` — done.
  Note: the introductory definitions at source lines 234–238.
  Dependencies: fixed-point and fixing subgroups.
- `Hypotheses` — done.
  Note: finite solvable even `G`, finite elementary abelian `V`, faithful action, and `O₂(G)=1`.
  Dependencies: `pCore` and fixing subgroup.
- `oddCore` — done.
  Note: corrected `W=O_{2'}(G)`.
  Dependencies: `pPrimeCore`.
- `LemmaOneOneConclusion` / `lemma_one_one` — done.
  Note: the four source clauses as named fields of one proof-valued conclusion structure.
  Dependencies: standing hypotheses, `oddCore`, action terminology.
- `actionClosure` — done.
  Note: subgroup generated by all action translates of a subgroup.
  Dependencies: `MulDistribMulAction`, subgroup closure.
- `lemma_one_two` — done.
  Note: fixed-point generation from the index-two subgroups of `S`.
  Dependencies: `actionClosure`, `IsQuadraticAction`, `oddCore`.
- `involutionCommutator` — done.
  Note: `F=[W,x]` in the ambient group.
  Dependencies: `oddCore`, subgroup commutator.
- `LemmaOneThreeConclusion` — done.
  Note: the three alternatives (a)–(c), including cyclic and extraspecial models.
  Dependencies: action commutators, `IsExtraspecial`.
- `lemma_one_three` — done.
  Note: the source trichotomy under the p-group and fixed-index hypotheses.
  Dependencies: `involutionCommutator`, `LemmaOneThreeConclusion`.

## Route Ledger

- `R1-action-interface` — done. Uses: `main`, action terminology, `Hypotheses`, `oddCore`, `LemmaOneOneConclusion`. Result: owner and wrapper builds succeed. Node disposition: keep. Decision/Lesson: apply repaired source finding `SF-stellmacher-1-odd-core`. Evidence: journal p. 14 scan.
- `R2-lemma-one-two` — done. Uses: `actionClosure`, `lemma_one_two`. Result: owner and wrapper builds succeed. Node disposition: keep. Decision/Lesson: none. Evidence: source lines 282–286.
- `R3-lemma-one-three` — done. Uses: `involutionCommutator`, `LemmaOneThreeConclusion`, `lemma_one_three`. Result: owner and wrapper builds succeed. Node disposition: keep. Decision/Lesson: map `Z(F)` into the ambient group before forming its commutator with `x`. Evidence: source lines 302–312 and targeted builds.

## Validation

- Last check: `lake build Stellmacher.SectionOne` and `lake build Stellmacher` succeeded at 2026-09-01T11:51:14Z.
- Final validation: closed for the statement-only milestone; the three requested theorem proofs remain `sorry`.

## Source Notes

- Audit status: standing hypotheses and Lemmas 1.1–1.3 statements audited.
- [transcription] `refs/latex/stellmacher-n-group.tex`:L249 has `W=O₂(G)`; issue: the scan shows `O_{2'}(G)` and later clauses require an odd core; impact: every occurrence of `W`; disposition: repaired statement; evidence/repair: global finding `SF-stellmacher-1-odd-core`, formalized as `pPrimeCore 2 G`.
