/-
Authors: OpenAI
-/

module

public import BenderSuzuki.External.Huppert.IV.Residual

/-!
# Huppert IV.3.2

Book-order entry for Huppert's abelian `p`-residual `G'(p)`.

The project definition is `hktAbelianPResidual p G`: the subgroup generated by
the ordinary commutator subgroup and the `p`-residual.  The theorems below
record the source meaning of IV.3.2: it is the smallest normal subgroup whose
quotient is both abelian and a `p`-group.

The parenthetical source reference to Huppert I.9.7 is covered here by the
least-property formulation; the general subgroup/direct-product closure facts
are available from the core quotient and product APIs.
-/

namespace BenderSuzuki
namespace External

universe u


end External
end BenderSuzuki
