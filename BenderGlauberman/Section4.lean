module

public import BenderGlauberman.Section4.Theorem43

/-!
# Bender--Glauberman: Section 4

The case `|S| = 4`: the generalized characters `δν := (ν−λ2ν)* = ν̃ − λ̃₂ν`,
the irreducible character `ν̂` of `B`, the set `B′(χ)`, and the graph `Δ` on
the `δν`. Lemmas 4.1 and 4.2 and Theorem 4.3 are proved in their own modules
and re-exported here.
-/
